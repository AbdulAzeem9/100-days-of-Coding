let pName :string = "Azeem"; // This saves the name
console.log(pName.toLowerCase()); //shows name in small letters
console.log(pName.toUpperCase()); //shows name in BIG LETTERS
console.log(pName.charAt(0).toUpperCase() + pName.slice(1).toLowerCase()); //shows name with the first letter BIG