let transports: string[]= ["V8", "TZ", "LX-570"]; // This saves the names

transports.forEach(transports => {
    console.log(`I would like to own a ${transports}.`);
}); // This prints all transport names with the same statement