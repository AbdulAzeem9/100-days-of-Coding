let Names: string[] = ["kashan", "muqeet", "wahaj"]; // This saves the name
for (let i = 0; i < Names.length; i++) {
    console.log(`Hi ${Names[i]}`); // This shows the name one by one with same message
}